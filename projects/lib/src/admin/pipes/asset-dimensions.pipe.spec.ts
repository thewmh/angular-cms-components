import { AssetDimensionsPipe } from './asset-dimensions.pipe';

describe('AssetDimensionsPipe', () => {
  it('create an instance', () => {
    const pipe = new AssetDimensionsPipe();
    expect(pipe).toBeTruthy();
  });
});
