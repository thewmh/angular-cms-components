import { CmsDragAndDropDirective } from './drag-and-drop.directive';

describe('DragAndDropDirective', () => {
  it('should create an instance', () => {
    const directive = new CmsDragAndDropDirective();
    expect(directive).toBeTruthy();
  });
});
